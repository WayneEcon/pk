# 09_econometric_analysis 模块依赖
# =======================================

# 核心依赖 (必需)
pandas>=1.3.0
numpy>=1.20.0
pathlib2>=2.3.0

# 计量经济学分析库 (强烈推荐)
statsmodels>=0.13.0
linearmodels>=4.25

# 可视化库 (推荐)
matplotlib>=3.5.0
seaborn>=0.11.0

# 开发与测试 (可选)
pytest>=6.0.0
pytest-cov>=2.12.0
jupyter>=1.0.0

# 性能优化 (可选)
numba>=0.56.0
scipy>=1.7.0

# 文档生成 (可选)
sphinx>=4.0.0
sphinx-rtd-theme>=1.0.0